{
  "name": "demo",
  "version": "0.0.1",
  "scripts": {
    "start": "mvn clean install && java -jar target/$(cat package.json | jq -r .name)-$(cat package.json | jq -r .version).jar",
    "docker": "docker build --build-arg JAR_FILE=target/$(cat package.json | jq -r .name)-$(cat package.json | jq -r .version).jar -t $(cat package.json | jq -r .name) ."
  }
}
